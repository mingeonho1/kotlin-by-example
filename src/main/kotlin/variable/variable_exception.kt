package src.main.kotlin.variable

fun main() {
    var e: Int  // 1
//    println(e)  // 2
}

/**
 * 1. 초기화하지 않고 변수를 선언했습니다.
 * 2. 초기화하지 않은 변수를 읽으려 하면 컴파일러가 Variable 'e' must be initialized라고 에러를 냅니다.
 */